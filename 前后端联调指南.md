# 酱酒研学平台前后端联调指南

## 目录
1. [环境要求
2. [快速启动
3. [后端配置
4. [前端配置
5. [接口联调
6. [常见问题
7. [服务访问地址

---

## 1. 环境要求

### 后端环境
- Python 3.8+
- MongoDB 4.0+（可选，内存模式无需MongoDB）
- Redis 6.0+（可选）

### 前端环境
- Node.js 16+
- npm 8+ 或 yarn

---

## 2. 快速启动

### 方式一：一键启动（推荐）

```bash
# 进入项目目录
cd 酱酒研学平台

# 给脚本添加执行权限
chmod +x start_all.sh stop_all.sh

# 一键启动前后端
bash start_all.sh

# 停止所有服务
bash stop_all.sh
```

### 方式二：分别启动

```bash
# 启动后端
cd backend
bash start.sh

# 新开终端，启动前端
cd frontend
bash start.sh
```

---

## 3. 后端配置

### 项目结构
```
backend/
├── app/
│   ├── __init__.py
│   ├── database.py      # 数据库连接配置
│   └── main.py         # FastAPI主程序
├── scripts/
│   └── generate_mock_data.py  # 模拟数据生成脚本
├── .env                 # 环境配置文件
├── requirements.txt      # Python依赖
└── start.sh          # 启动脚本
```

### 配置说明（.env）

```env
# 服务配置
HOST=0.0.0.0
PORT=8000
DEBUG=True

# MongoDB配置
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=jiangjiu_research

# Redis配置
REDIS_URL=redis://localhost:6379/0
```

### MongoDB连接说明

- **有MongoDB时：数据持久化存储
- **无MongoDB时**：自动切换到内存模式，数据不持久化
- **内存模式优点**：无需安装数据库，快速启动，适合前端联调

### 模拟数据

自动生成以下模拟数据：
- 知识库文章：20篇
- 产区数据：5个
- 酒厂名录：50家
- 行业资讯：30条

数据内容涵盖酱酒文化、酿造工艺、品鉴指南、产区介绍等方面。

---

## 4. 前端配置

### 项目结构
```
frontend/
├── src/
│   ├── api/
│   │   └── index.js      # API接口配置
│   ├── views/            # 页面组件
│   ├── components/       # 公共组件
│   ├── router/          # 路由配置
│   ├── stores/          # Pinia状态管理
│   └── styles/          # 样式文件
├── .env                  # 环境变量
├── vite.config.js       # Vite配置
├── package.json        # 依赖配置
└── start.sh           # 启动脚本
```

### 代理配置（vite.config.js）

```javascript
server: {
  port: 3000,
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
      rewrite: (path) => path.replace(/^\/api/, '')
    }
  }
}
```

### API地址配置

前端API请求地址：
- 开发环境：通过代理 `/api` 转发到后端 `http://localhost:8000`
- 生产环境：根据实际部署地址配置

---

## 5. 接口联调测试

### 5.1 首页数据看板

**接口地址**：`GET /dashboard`

**功能说明**：获取首页统计数据、最新资讯、热门酒厂

**返回数据结构**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "statistics": {
      "knowledge_count": 20,
      "production_count": 5,
      "wineries_count": 50,
      "news_count": 30
    },
    "latest_news": [...],
    "hot_wineries": [...]
  }
}
```

### 5.2 知识库接口

**获取列表**：`GET /knowledge?category=&page=1&pageSize=10`

**获取详情**：`GET /knowledge/{id}`

**获取分类**：`GET /knowledge/categories`

**分类说明**：
- `culture`：酱酒文化
- `craft`：酿造工艺
- `tasting`：品鉴指南
- `production`：产区介绍

### 5.3 产区数据接口

**获取列表**：`GET /production?page=1&pageSize=10`

**获取详情**：`GET /production/{id}`

**统计数据**：`GET /production/statistics`

### 5.4 酒厂名录接口

**获取列表**：`GET /wineries?production=&page=1&pageSize=10`

**获取详情**：`GET /wineries/{id}`

### 5.5 资讯接口

**获取列表**：`GET /news?type=&page=1&pageSize=10`

**获取详情**：`GET /news/{id}`

**资讯类型**：
- `policy`：政策动态
- `trend`：行业趋势
- `company`：企业新闻
- `event`：展会活动

### 5.6 搜索接口

**全站搜索**：`GET /search?keyword=`

**热门搜索**：`GET /search/hot`

### 5.7 用户接口

**用户信息**：`GET /user/profile`

**收藏列表**：`GET /user/favorites`

**浏览历史**：`GET /user/history`

---

## 6. 常见问题排查

### 6.1 后端启动问题

**问题1：端口被占用
```
ERROR:    [Errno 98] Address already in use
```
解决：
```bash
# 查找占用端口进程
lsof -i :8000
# 杀死进程
kill -9 <PID>
```

**问题2：MongoDB连接失败
解决：后端会自动切换到内存模式，不影响联调

**问题3：依赖安装失败
解决：
```bash
pip install --upgrade pip
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 6.2 前端启动问题

**问题1：端口被占用
解决：修改 `vite.config.js` 中的 `port` 配置

**问题2：依赖安装失败
解决：
```bash
# 清理缓存
rm -rf node_modules package-lock.json
# 重新安装
npm install
# 或使用淘宝源
npm install --registry=https://registry.npmmirror.com
```

**问题3：接口请求失败
检查：
1. 后端服务是否启动
2. 代理配置是否正确
3. 浏览器控制台Network查看错误信息

### 6.3 联调问题

**问题1：跨域问题
解决：后端已配置CORS允许所有来源访问

**问题2：数据不显示
排查：
1. 检查浏览器控制台是否有报错
2. 检查Network请求是否成功
3. 检查数据格式是否匹配

---

## 7. 服务访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端页面 | http://localhost:3000 | 主应用入口 |
| 后端API | http://localhost:8000 | API服务地址 |
| API文档 | http://localhost:8000/docs | Swagger文档 |
| Redoc文档 | http://localhost:8000/redoc | Redoc文档 |

---

## 8. 开发建议

### 开发流程
1. 启动后端服务
2. 启动前端服务
3. 打开浏览器访问前端页面
4. 测试各功能模块
5. 根据需要调整接口或页面

### 调试技巧
1. 使用浏览器开发者工具Network面板查看接口请求
2. 使用Swagger文档（http://localhost:8000/docs）测试接口
3. 后端日志输出在终端查看
4. 前端使用Vue DevTools调试

---

## 9. 部署说明

### 后端部署
```bash
# 生产环境启动
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### 前端部署
```bash
# 构建生产版本
npm run build

# dist目录即为部署产物
```

---

**最后更新：2024年5月
